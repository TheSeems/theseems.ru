/** Single source of truth for primary nav / scroll-spy sections. */
export const sections = [
  { id: "home", label: "Home" },
  { id: "projects", label: "Projects" },
  { id: "contact", label: "Contact" },
];

export const sectionIds = sections.map((s) => s.id);
